import React from 'react';
import Layout from '../components/layout';

export default () => (
    <Layout>
        <div>
            <h1>About me</h1>
            <p>im good enough, im smart enough, and gosh darn it, people like me.</p>
        </div>
    </Layout>
);